redis:
  image: redis:3.0.5

mongo:
  image: mongo:3.2.0
  volumes:
    - ./backups:/backups

server:
  extends:
    file: ./docker-compose.base.yml
    service: server
  links:
    - redis
    - mongo
